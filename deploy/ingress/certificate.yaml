apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: substrate-tls
  namespace: substrate
spec:
  secretName: substrate-tls
  dnsNames:
    - substrate.<tailnet>
  issuerRef:
    name: "<cluster-issuer>"
    kind: ClusterIssuer
