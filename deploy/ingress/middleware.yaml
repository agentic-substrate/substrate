apiVersion: traefik.io/v1alpha1
kind: Middleware
metadata:
  name: mcp-per-token-ratelimit
  namespace: substrate
spec:
  rateLimit:
    average: 60
    period: 1m
    burst: 30
    sourceCriterion:
      requestHeaderName: Authorization
